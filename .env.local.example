# Copy to .env.local (gitignored). Frontend reads this at build/dev time.
# Get the value from Google Cloud Console -> Credentials -> OAuth client ID (Web).
VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
