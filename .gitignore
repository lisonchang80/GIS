# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
# deploy.bat staging + rollback copies (built artifacts, never committed)
dist_new
dist_prev
dist_bad
*.local

# Backend (FastAPI + SQLite)
server/.venv
server/__pycache__
server/gis.db
server/.env
.env.local

# Deploy secrets (filled locally, never committed)
packaging/env.local.bat

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 本機預設範本來源匯出檔（實際使用的種子在 server/seed_demo.json）
/Demo/
